@page
@model BookListRazor.Pages.AuthorList.UpsertModel
@{
}

<br />
<h2 class="text-info">@(Model.Author.Id != 0 ? "Edit":"Create")</h2>
<br />

<div class="border container" style="padding:30px;">
    <form method="post">
        @if (Model.Author.Id != 0)
        {
            <input type="hidden" asp-for="Author.Id" />
        }
        <div class="text-danger" asp-validation-summary="ModelOnly"></div>
        <div class="form-group row">
            <div class="col-3">
                <label asp-for="Author.AuthorName"></label>
            </div>
            <div class="col-6">
                <input asp-for="Author.AuthorName" class="form-control" />
            </div>
            <span asp-validation-for="Author.AuthorName" class="text-danger"></span>
        </div>
        <div class="form-group row">
            <div class="col-3 offset-3">
                <button class="btn btn-primary form-control" type="submit">@(Model.Author.Id != 0 ? "Update":"Create")</button>
            </div>
            <div class="col-3">
                <a asp-page="Index" class="btn btn-success form-control">Back To List</a>
            </div>
        </div>
    </form>
</div>
